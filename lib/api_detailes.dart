import 'package:flutter/material.dart';

class ApiDetailes extends StatelessWidget {
  const ApiDetailes({super.key});
  // List<Photos> photoList;
  // ApiDetailes(this.photoList);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
